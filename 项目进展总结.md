# 项目进展总结（space-know）

## 已完成

- 完成基础项目搭建：`React + Vite + Three.js`（含 `@react-three/fiber` / `@react-three/drei`）。
- 实现正方体展开图交互教学页面（当前在 `src/pages/CubePage.jsx`）：
  - 支持 11 种正方体展开图切换。
  - 支持展开/折叠动画与滑条控制。
  - 支持面点击高亮与对面关系提示。
  - 支持“猜一猜”对面关系小游戏。
- 优化“排队切换”体验：
  - 点击后立即切换，不再等待长动画。
- 实现“平面状态点击折面”能力并持续优化：
  - 初版支持单面折叠/展开。
  - 已升级为多面独立状态（每个面状态互不覆盖）。
  - 加入短按触发、拖动/长按不触发切换，避免和旋转手势冲突。
- 删除了右侧不必要的底部按钮区（前面/后面/上面/下面/左面/右面那组）。
- 新增首页与详情入口流转（`src/HomeApp.jsx`）：
  - 首页展示多个立体卡片（正方体、长方体、圆柱体、圆锥体）。
  - 点击正方体进入完整详情页。
  - 其他立体进入占位详情页（结构已预留）。
  - 在正方体详情页加“返回首页”按钮。
- 完成 Git 仓库初始化、提交与推送：
  - 远程仓库：`https://github.com/chending459166184-bit/space-know.git`
  - 当前主分支：`main`

## 还没做（待办）

- 长方体详情页：暂无真实交互（目前是占位页）。
- 圆柱体详情页：暂无真实交互（目前是占位页）。
- 圆锥体详情页：暂无真实交互（目前是占位页）。
- 首页与详情页的正式路由化（目前是组件内状态切换，不是 URL 路由）。
- 统一“返回首页”入口样式与交互细节（当前已可用，但可继续打磨）。
- 教学内容体系化（按年龄段、课程节奏、关卡难度分层）。

## 建议下一步

1. 先做长方体详情页（与正方体逻辑最接近，复用率最高）。
2. 抽象一层形体通用引擎（`shape data + fold math + quiz config`）。
3. 再做圆柱体（引入曲面展开），最后做圆锥体（扇形展开）。
4. 引入正式路由（如 `react-router`）支持可分享链接：
   - `/` 首页
   - `/cube` 正方体
   - `/cuboid` 长方体
   - `/cylinder` 圆柱体
   - `/cone` 圆锥体
5. 为关键交互补少量回归测试（点击/拖动判定、状态独立折叠等）。

## 当前关键文件

- 首页壳：`src/HomeApp.jsx`
- 正方体详情页：`src/pages/CubePage.jsx`
- 入口：`src/main.jsx`
- 样式：`src/App.css`
- 展开图数据：`src/data/cubeNets.js`
- 几何与关系计算：`src/lib/cubeMath.js`
